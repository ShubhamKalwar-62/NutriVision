# Auto-detect text files and normalise line endings to LF in the repo
* text=auto eol=lf

# Explicitly declare binary files so Git never tries to diff them
*.weights  binary
*.png      binary
*.jpg      binary
*.jpeg     binary
*.gif      binary
*.ico      binary
*.svg      text eol=lf
